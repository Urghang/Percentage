var button = document.getElementById("submitbutton");
var element = document.getElementById("spinner");

button.addEventListener("click", function() {i
  element.classList.remove("fa-spin");  // добавление класса в элемент
});
